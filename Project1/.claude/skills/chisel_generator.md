# Chisel 代码生成技能

## 触发条件
当用户要求生成 RISC-V 模块（如 ALU、寄存器文件等）时，自动使用此技能。

## 执行步骤
1. 解析需求，确定模块名称、端口、功能。
2. 生成符合 RV32I 规范的 Chisel 代码，放置于 `src/` 目录。
3. 代码应包含 `import chisel3._`，并遵循标准硬件描述风格。
4. 若需要时钟和复位，端口命名为 `clock` 和 `reset`（均为 Bool 类型）。
5. 使用 `printf` 或仿真辅助逻辑以支持测试。